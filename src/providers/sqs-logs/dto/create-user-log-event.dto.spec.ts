import { CreateUserLogEventDto } from './create-user-log-event.dto';

describe('CreateUserLogEventDto', () => {
  it('should be defined', () => {
    expect(new CreateUserLogEventDto()).toBeDefined();
  });
});
