import { CreateAccountLogEventDto } from './create-account-log-event.dto';

describe('CreateAccountLogEventDto', () => {
  it('should be defined', () => {
    expect(new CreateAccountLogEventDto()).toBeDefined();
  });
});
